const $=s=>document.querySelector(s),money=n=>new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(n);
const statusText={menunggu_konfirmasi:"Menunggu Konfirmasi",diproses:"Diproses",selesai:"Selesai",dibatalkan:"Dibatalkan",pending:"Menunggu Pembayaran"};
const payText={transfer:"Transfer",cash:"Cash"};
async function init(){const m=await(await fetch("/api/me")).json();if(!m.user)return location="/login.html";const [r,qr]=await Promise.all([fetch("/api/orders"),fetch("/api/payment-settings")]);const o=await r.json(),d=await qr.json();render(o,d.dana_qr_url||"")}
function render(os,qrUrl){$("#orders").innerHTML=os.map(o=>`<div class="order"><div><b>Pesanan #${o.id}</b> <span class="badge">${statusText[o.status]||o.status}</span></div><p>Pembayaran: <b>${payText[o.payment_method]||o.payment_method}</b> · ${o.payment_status==="confirmed"?"Dikonfirmasi":"Menunggu konfirmasi"}</p><div>${o.items.map(i=>`${esc(i.name)} × ${i.quantity} — ${money(i.price*i.quantity)}`).join("<br>")}</div><h3>Total: ${money(o.total)}</h3>${o.payment_method==="transfer"&&o.status==="menunggu_konfirmasi"?`<div class="order-qr"><p><b>Scan QR DANA terbaru untuk pembayaran:</b></p>${qrUrl?`<img class="dana-qr-img" src="${esc(qrUrl)}" alt="QR DANA">`:"<p>QR DANA belum diatur admin.</p>"}</div>`:""}</div>`).join("")||"<p>Belum ada pesanan.</p>"}
function esc(s){return String(s||"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}init();