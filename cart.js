let products=[],cart=JSON.parse(localStorage.cart||"[]");const $=s=>document.querySelector(s),money=n=>new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(n);
async function init(){products=await(await fetch("/api/products")).json();render()}
function render(){let total=0;$("#cart").innerHTML=cart.map(x=>{const p=products.find(p=>p.id===x.id);if(!p)return"";total+=p.price*x.qty;return`<div class="cartrow"><div><b>${p.name}</b><br>${money(p.price)} × ${x.qty}</div><div><button onclick="change(${p.id},-1)">−</button> ${x.qty} <button onclick="change(${p.id},1)">+</button></div><div>${money(p.price*x.qty)}</div></div>`}).join("")+`<h2>Total: ${money(total)}</h2>`}
function change(id,n){const x=cart.find(a=>a.id===id);x.qty+=n;if(x.qty<1)cart=cart.filter(a=>a.id!==id);localStorage.cart=JSON.stringify(cart);render()}
$("#order").onclick=async()=>{const m=await(await fetch("/api/me")).json();if(!m.user)return location="/login.html";const r=await fetch("/api/orders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({customerName:$("#name").value,phone:$("#phone").value,address:$("#address").value,items:cart})}),d=await r.json();if(!r.ok)return alert(d.error);localStorage.cart="[]";alert("Pesanan #"+d.orderId+" dibuat.");location="/"};init();